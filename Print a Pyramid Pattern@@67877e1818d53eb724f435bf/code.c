#include<stdio.h>

int min() {
    int N;
    scanf("%d", &N);
    for(int i = N; i <= N i++) {
        for(int j = i; j <N; j++ ) {
            printf(" ");
        }
            for(int k =1; k <= i; k++) {
                printf("*");
            }
        printf("\n");
    }
    return 0;
}
